<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.hsa.dinder">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dinder"
        tools:targetApi="31">
        <activity
            android:name=".Register2"
            android:exported="true"
            android:label="@string/register_de"/>
        <activity
            android:name=".Register1"
            android:exported="true"
            android:label="@string/register_de"/>
        <activity
            android:name=".Login"
            android:exported="true"
            android:label="@string/login_de"/>
        <activity
            android:name=".TitleScreen"
            android:exported="true"
            android:label="Dinder">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>